dump hello(arg)
{
        assert(out(arg - 12));
        return arg;
}

dump main() 
{
        assert(b = in());
        while (b > 0) {
                assert(b = b - 1);
                assert(hello(b));
        }
        
        return 0;
}
